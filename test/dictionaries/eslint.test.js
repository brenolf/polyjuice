var eslint = require('../../lib/dictionaries/eslint')

var getFn = caller(eslint)

describe('eslint', function () {
  it.skip('converts comma-dangle correctly', function () {
    var fn = getFn('comma-dangle')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-cond-assign correctly', function () {
    var fn = getFn('no-cond-assign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-console correctly', function () {
    var fn = getFn('no-console')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-constant-condition correctly', function () {
    var fn = getFn('no-constant-condition')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-control-regex correctly', function () {
    var fn = getFn('no-control-regex')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-debugger correctly', function () {
    var fn = getFn('no-debugger')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-dupe-args correctly', function () {
    var fn = getFn('no-dupe-args')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-dupe-keys correctly', function () {
    var fn = getFn('no-dupe-keys')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-duplicate-case correctly', function () {
    var fn = getFn('no-duplicate-case')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-empty-character-class correctly', function () {
    var fn = getFn('no-empty-character-class')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-empty correctly', function () {
    var fn = getFn('no-empty')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-ex-assign correctly', function () {
    var fn = getFn('no-ex-assign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-extra-boolean-cast correctly', function () {
    var fn = getFn('no-extra-boolean-cast')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-extra-parens correctly', function () {
    var fn = getFn('no-extra-parens')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-extra-semi correctly', function () {
    var fn = getFn('no-extra-semi')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-func-assign correctly', function () {
    var fn = getFn('no-func-assign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-inner-declarations correctly', function () {
    var fn = getFn('no-inner-declarations')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-invalid-regexp correctly', function () {
    var fn = getFn('no-invalid-regexp')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-irregular-whitespace correctly', function () {
    var fn = getFn('no-irregular-whitespace')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-negated-in-lhs correctly', function () {
    var fn = getFn('no-negated-in-lhs')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-obj-calls correctly', function () {
    var fn = getFn('no-obj-calls')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-regex-spaces correctly', function () {
    var fn = getFn('no-regex-spaces')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-sparse-arrays correctly', function () {
    var fn = getFn('no-sparse-arrays')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-unexpected-multiline correctly', function () {
    var fn = getFn('no-unexpected-multiline')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-unreachable correctly', function () {
    var fn = getFn('no-unreachable')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts use-isnan correctly', function () {
    var fn = getFn('use-isnan')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts valid-jsdoc correctly', function () {
    var fn = getFn('valid-jsdoc')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts valid-typeof correctly', function () {
    var fn = getFn('valid-typeof')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts accessor-pairs correctly', function () {
    var fn = getFn('accessor-pairs')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts block-scoped-var correctly', function () {
    var fn = getFn('block-scoped-var')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts complexity correctly', function () {
    var fn = getFn('complexity')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts consistent-return correctly', function () {
    var fn = getFn('consistent-return')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts curly correctly', function () {
    var fn = getFn('curly')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts default-case correctly', function () {
    var fn = getFn('default-case')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts dot-location correctly', function () {
    var fn = getFn('dot-location')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts dot-notation correctly', function () {
    var fn = getFn('dot-notation')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts eqeqeq correctly', function () {
    var fn = getFn('eqeqeq')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts guard-for-in correctly', function () {
    var fn = getFn('guard-for-in')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-alert correctly', function () {
    var fn = getFn('no-alert')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-caller correctly', function () {
    var fn = getFn('no-caller')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-div-regex correctly', function () {
    var fn = getFn('no-div-regex')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-else-return correctly', function () {
    var fn = getFn('no-else-return')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-empty-label correctly', function () {
    var fn = getFn('no-empty-label')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-empty-pattern correctly', function () {
    var fn = getFn('no-empty-pattern')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-eq-null correctly', function () {
    var fn = getFn('no-eq-null')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-eval correctly', function () {
    var fn = getFn('no-eval')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-extend-native correctly', function () {
    var fn = getFn('no-extend-native')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-extra-bind correctly', function () {
    var fn = getFn('no-extra-bind')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-fallthrough correctly', function () {
    var fn = getFn('no-fallthrough')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-floating-decimal correctly', function () {
    var fn = getFn('no-floating-decimal')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-implicit-coercion correctly', function () {
    var fn = getFn('no-implicit-coercion')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-implied-eval correctly', function () {
    var fn = getFn('no-implied-eval')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-invalid-this correctly', function () {
    var fn = getFn('no-invalid-this')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-iterator correctly', function () {
    var fn = getFn('no-iterator')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-labels correctly', function () {
    var fn = getFn('no-labels')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-lone-blocks correctly', function () {
    var fn = getFn('no-lone-blocks')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-loop-func correctly', function () {
    var fn = getFn('no-loop-func')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-magic-numbers correctly', function () {
    var fn = getFn('no-magic-numbers')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-multi-spaces correctly', function () {
    var fn = getFn('no-multi-spaces')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-multi-str correctly', function () {
    var fn = getFn('no-multi-str')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-native-reassign correctly', function () {
    var fn = getFn('no-native-reassign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-new-func correctly', function () {
    var fn = getFn('no-new-func')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-new-wrappers correctly', function () {
    var fn = getFn('no-new-wrappers')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-new correctly', function () {
    var fn = getFn('no-new')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-octal-escape correctly', function () {
    var fn = getFn('no-octal-escape')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-octal correctly', function () {
    var fn = getFn('no-octal')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-param-reassign correctly', function () {
    var fn = getFn('no-param-reassign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-process-env correctly', function () {
    var fn = getFn('no-process-env')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-proto correctly', function () {
    var fn = getFn('no-proto')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-redeclare correctly', function () {
    var fn = getFn('no-redeclare')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-return-assign correctly', function () {
    var fn = getFn('no-return-assign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-script-url correctly', function () {
    var fn = getFn('no-script-url')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-self-compare correctly', function () {
    var fn = getFn('no-self-compare')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-sequences correctly', function () {
    var fn = getFn('no-sequences')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-throw-literal correctly', function () {
    var fn = getFn('no-throw-literal')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-unused-expressions correctly', function () {
    var fn = getFn('no-unused-expressions')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-useless-call correctly', function () {
    var fn = getFn('no-useless-call')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-useless-concat correctly', function () {
    var fn = getFn('no-useless-concat')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-void correctly', function () {
    var fn = getFn('no-void')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-warning-comments correctly', function () {
    var fn = getFn('no-warning-comments')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-with correctly', function () {
    var fn = getFn('no-with')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts radix correctly', function () {
    var fn = getFn('radix')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts vars-on-top correctly', function () {
    var fn = getFn('vars-on-top')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts wrap-iife correctly', function () {
    var fn = getFn('wrap-iife')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts yoda correctly', function () {
    var fn = getFn('yoda')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts strict correctly', function () {
    var fn = getFn('strict')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts init-declarations correctly', function () {
    var fn = getFn('init-declarations')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-catch-shadow correctly', function () {
    var fn = getFn('no-catch-shadow')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-delete-var correctly', function () {
    var fn = getFn('no-delete-var')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-label-var correctly', function () {
    var fn = getFn('no-label-var')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-shadow-restricted-names correctly', function () {
    var fn = getFn('no-shadow-restricted-names')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-shadow correctly', function () {
    var fn = getFn('no-shadow')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-undef-init correctly', function () {
    var fn = getFn('no-undef-init')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-undef correctly', function () {
    var fn = getFn('no-undef')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-undefined correctly', function () {
    var fn = getFn('no-undefined')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-unused-vars correctly', function () {
    var fn = getFn('no-unused-vars')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-use-before-define correctly', function () {
    var fn = getFn('no-use-before-define')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts callback-return correctly', function () {
    var fn = getFn('callback-return')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts global-require correctly', function () {
    var fn = getFn('global-require')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts handle-callback-err correctly', function () {
    var fn = getFn('handle-callback-err')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-mixed-requires correctly', function () {
    var fn = getFn('no-mixed-requires')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-new-require correctly', function () {
    var fn = getFn('no-new-require')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-path-concat correctly', function () {
    var fn = getFn('no-path-concat')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-process-exit correctly', function () {
    var fn = getFn('no-process-exit')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-restricted-modules correctly', function () {
    var fn = getFn('no-restricted-modules')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-sync correctly', function () {
    var fn = getFn('no-sync')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts array-bracket-spacing correctly', function () {
    var fn = getFn('array-bracket-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts block-spacing correctly', function () {
    var fn = getFn('block-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts brace-style correctly', function () {
    var fn = getFn('brace-style')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts camelcase correctly', function () {
    var fn = getFn('camelcase')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts comma-spacing correctly', function () {
    var fn = getFn('comma-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts comma-style correctly', function () {
    var fn = getFn('comma-style')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts computed-property-spacing correctly', function () {
    var fn = getFn('computed-property-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts consistent-this correctly', function () {
    var fn = getFn('consistent-this')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts eol-last correctly', function () {
    var fn = getFn('eol-last')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts func-names correctly', function () {
    var fn = getFn('func-names')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts func-style correctly', function () {
    var fn = getFn('func-style')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts id-length correctly', function () {
    var fn = getFn('id-length')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts id-match correctly', function () {
    var fn = getFn('id-match')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts indent correctly', function () {
    var fn = getFn('indent')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts jsx-quotes correctly', function () {
    var fn = getFn('jsx-quotes')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts key-spacing correctly', function () {
    var fn = getFn('key-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts linebreak-style correctly', function () {
    var fn = getFn('linebreak-style')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts lines-around-comment correctly', function () {
    var fn = getFn('lines-around-comment')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts max-nested-callbacks correctly', function () {
    var fn = getFn('max-nested-callbacks')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts new-cap correctly', function () {
    var fn = getFn('new-cap')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts new-parens correctly', function () {
    var fn = getFn('new-parens')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts newline-after-var correctly', function () {
    var fn = getFn('newline-after-var')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-array-constructor correctly', function () {
    var fn = getFn('no-array-constructor')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-continue correctly', function () {
    var fn = getFn('no-continue')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-inline-comments correctly', function () {
    var fn = getFn('no-inline-comments')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-lonely-if correctly', function () {
    var fn = getFn('no-lonely-if')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-mixed-spaces-and-tabs correctly', function () {
    var fn = getFn('no-mixed-spaces-and-tabs')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-multiple-empty-lines correctly', function () {
    var fn = getFn('no-multiple-empty-lines')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-negated-condition correctly', function () {
    var fn = getFn('no-negated-condition')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-nested-ternary correctly', function () {
    var fn = getFn('no-nested-ternary')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-new-object correctly', function () {
    var fn = getFn('no-new-object')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-restricted-syntax correctly', function () {
    var fn = getFn('no-restricted-syntax')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-spaced-func correctly', function () {
    var fn = getFn('no-spaced-func')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-ternary correctly', function () {
    var fn = getFn('no-ternary')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-trailing-spaces correctly', function () {
    var fn = getFn('no-trailing-spaces')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-underscore-dangle correctly', function () {
    var fn = getFn('no-underscore-dangle')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-unneeded-ternary correctly', function () {
    var fn = getFn('no-unneeded-ternary')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts object-curly-spacing correctly', function () {
    var fn = getFn('object-curly-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts one-var correctly', function () {
    var fn = getFn('one-var')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts operator-assignment correctly', function () {
    var fn = getFn('operator-assignment')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts operator-linebreak correctly', function () {
    var fn = getFn('operator-linebreak')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts padded-blocks correctly', function () {
    var fn = getFn('padded-blocks')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts quote-props correctly', function () {
    var fn = getFn('quote-props')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts quotes correctly', function () {
    var fn = getFn('quotes')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts require-jsdoc correctly', function () {
    var fn = getFn('require-jsdoc')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts semi-spacing correctly', function () {
    var fn = getFn('semi-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts semi correctly', function () {
    var fn = getFn('semi')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts sort-vars correctly', function () {
    var fn = getFn('sort-vars')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-after-keywords correctly', function () {
    var fn = getFn('space-after-keywords')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-before-blocks correctly', function () {
    var fn = getFn('space-before-blocks')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-before-function-paren correctly', function () {
    var fn = getFn('space-before-function-paren')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-before-keywords correctly', function () {
    var fn = getFn('space-before-keywords')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-in-parens correctly', function () {
    var fn = getFn('space-in-parens')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-infix-ops correctly', function () {
    var fn = getFn('space-infix-ops')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-return-throw-case correctly', function () {
    var fn = getFn('space-return-throw-case')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts space-unary-ops correctly', function () {
    var fn = getFn('space-unary-ops')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts spaced-comment correctly', function () {
    var fn = getFn('spaced-comment')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts wrap-regex correctly', function () {
    var fn = getFn('wrap-regex')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts arrow-parens correctly', function () {
    var fn = getFn('arrow-parens')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts arrow-spacing correctly', function () {
    var fn = getFn('arrow-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts constructor-super correctly', function () {
    var fn = getFn('constructor-super')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts generator-star-spacing correctly', function () {
    var fn = getFn('generator-star-spacing')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-class-assign correctly', function () {
    var fn = getFn('no-class-assign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-const-assign correctly', function () {
    var fn = getFn('no-const-assign')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-dupe-class-members correctly', function () {
    var fn = getFn('no-dupe-class-members')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-this-before-super correctly', function () {
    var fn = getFn('no-this-before-super')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-var correctly', function () {
    var fn = getFn('no-var')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts object-shorthand correctly', function () {
    var fn = getFn('object-shorthand')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts prefer-arrow-callback correctly', function () {
    var fn = getFn('prefer-arrow-callback')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts prefer-const correctly', function () {
    var fn = getFn('prefer-const')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts prefer-reflect correctly', function () {
    var fn = getFn('prefer-reflect')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts prefer-spread correctly', function () {
    var fn = getFn('prefer-spread')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts prefer-template correctly', function () {
    var fn = getFn('prefer-template')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts require-yield correctly', function () {
    var fn = getFn('require-yield')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts max-depth correctly', function () {
    var fn = getFn('max-depth')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts max-len correctly', function () {
    var fn = getFn('max-len')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts max-params correctly', function () {
    var fn = getFn('max-params')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts max-statements correctly', function () {
    var fn = getFn('max-statements')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-bitwise correctly', function () {
    var fn = getFn('no-bitwise')

    expect(fn()).to.eql(
      {}
    )
  })

  it.skip('converts no-plusplus correctly', function () {
    var fn = getFn('no-plusplus')

    expect(fn()).to.eql(
      {}
    )
  })
})
